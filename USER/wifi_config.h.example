#ifndef WIFI_CONFIG_H
#define WIFI_CONFIG_H

/*
 * Copy this file to wifi_config.h, then fill in your own values.
 * wifi_config.h is ignored by Git so your Wi-Fi password is not uploaded.
 */
#define ESP_WIFI_SSID      "YOUR_WIFI_SSID"
#define ESP_WIFI_PASSWORD  "YOUR_WIFI_PASSWORD"
#define ESP_SERVER_IP      "192.168.1.100"
#define ESP_SERVER_PORT    "8080"

#endif
